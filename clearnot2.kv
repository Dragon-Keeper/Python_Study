#:kivy 1.8.0
#:import datetime datetime

RootWidget:
    # import container
    container: container

    # fill the container
    BoxLayout:
        id: container
        orientation: 'horizontal'
        padding: 250  # decide Label's Y_position
        spacing: 300

        Label:
            # text: 'Clear Or Not'
            font_size:50
